package MISC;

/**
 * Auslistung aller Ideen und Aufgaben zur Anzeige in der IDE.
 * 
 * @author executor
 *
 */

public class Todo {
	// TODO für den nächsten Release:
	/*
	 * - Interface für Filter URLs (Filter für mehrere Seiten)
	 * - Interface für Suche (Suche auf mehreren Seiten, Tabelle ähnlich IRC)
	 * DONE - Architektur für Downloads und Warteschlange überarbeiten
	 * DONE - Queue mit Prioritäten
	 * DONE - Optionen bei Downloadlisten: für alle/ einzelne, Warten und Priorität +/- hinzufügen
	 * - IRC überarbeiten: Downloadordner, Downloadende, Log (3x mal start...), Abbrechen(Programmabsturz)
	 * - Speedanzeige & -algorithmus überarbeiten
	 * - Implementierung einer Suche & Filter um Interface dafür zu testen
	 * - Download Save-File als XML
	 */
	
	//  TODO von Executor
	// Eigene Language-File angeben und laden
	// Öffnen mit "PnF" realisieren. In Middleware argv abfragen und ggf. laden
	// Fehler: Nach 2mal Download stoppen, keiner Starten möglich bei MegaUpload
	// Suche für ddl-warez.org, oxygen-warez.org, mediaportal.ru, megadownload.pl, ddl-music.org, serienjunkies.org, 3dl.am
	// Filter für ...
	
	// TODO von Acid
	// PnF in Systray minimieren lassen
	// bei mouseover über systray soll eine übersicht erscheinen, in der alle Hoster aufgeführt sind. Dabei soll Angezeigt werden: Hoster | xx% downloaded | act. speed | restliche dateien
	// automatisches Auslesen von linksave-seiten und extrahieren aller Links
	// anzeige der aktuell herunter zu ladenen Dateien (möglicherweise in tabellenkopf neben "Dateiname")
	// dateinamen bei irc speichern, sodass sie beim neustart des programms wieder angezeigt werden
	// filter implementieren
	// (vielleicht) In Spalte "Download" den Pfad und den Dateinamen eintragen, wo die Datei gespeichert wurde. Das vermittelt ein besseres Gefühl dafür, wo am ende die Dateien liegen.
	// In den Einstellungen sollte man einstellen können, ob downloads automatisch beim programmstart gestartet werden sollen, oder nicht.
	// spaltenbreiten anpassen
	
}
